#include <stdio.h>

int main()
{
  const pi = 3.14159
  printf("%f\n", pi);
  return 0;
}
// constants in c are variables whose vlaue cannot be changed in a program once declared
// e.g. pi is a constant and it's value need to be same no matter what
//and for that we use "const" keyword to declare a constant
